---
page_title: "Core: synology_core_firewall"
subcategory: "Core"
description: |-
  Manages DSM firewall enablement, the active profile name, and port-check conf (SYNO.Core.Security.Firewall + .Conf).
  Break-glass: enabling the firewall with an empty / default-deny profile locks out DSM (including SSH). Create allow rules on the profile before enabling, and prefer importing the live config rather than inventing rules from scratch. DS1621+ Mode 1 reset restores network settings if locked out.
  Some service accounts receive DSM error 114 on Firewall.set even when Firewall.get works; profile rules still manage via synology_core_firewall_profile.
---

# Core: Firewall (Resource)

Manages DSM firewall enablement, the active profile name, and port-check conf (`SYNO.Core.Security.Firewall` + `.Conf`).

**Break-glass:** enabling the firewall with an empty / default-deny profile locks out DSM (including SSH). Create allow rules on the profile *before* enabling, and prefer importing the live config rather than inventing rules from scratch. DS1621+ Mode 1 reset restores network settings if locked out.

Some service accounts receive DSM error 114 on `Firewall.set` even when `Firewall.get` works; profile rules still manage via `synology_core_firewall_profile`.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `profile_name` (String) Active firewall profile name (must already exist).

### Optional

- `enable` (Boolean) Whether the firewall is enabled.
- `enable_port_check` (Boolean) Whether DSM firewall port-check is enabled.

### Read-Only

- `id` (String) Singleton id (`firewall`).
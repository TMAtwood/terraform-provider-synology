---
page_title: "Core: synology_core_firewall_profile"
subcategory: "Core"
description: |-
  Manages a DSM firewall profile document (adapters + ordered rules) via SYNO.Core.Security.Firewall.Profile set and optional Profile.Apply two-phase commit.
  Break-glass: never apply a default-drop profile with no allow rules — that locks out DSM/SSH. Import the live profile first. New profile names may need to be created once in Control Panel (DSM returns 117 for some create paths); afterward set/apply work for updates.
  DS1621+ Mode 1 reset restores network settings if locked out.
---

# Core: Firewall Profile (Resource)

Manages a DSM firewall profile document (adapters + ordered rules) via `SYNO.Core.Security.Firewall.Profile` set and optional `Profile.Apply` two-phase commit.

**Break-glass:** never apply a default-drop profile with no allow rules — that locks out DSM/SSH. Import the live profile first. New profile *names* may need to be created once in Control Panel (DSM returns 117 for some create paths); afterward set/apply work for updates.

DS1621+ Mode 1 reset restores network settings if locked out.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Profile name.

### Optional

- `adapter` (Block List) Per-adapter policy and ordered rules. Use `global` for all-interface rules (typical). (see [below for nested schema](#nestedblock--adapter))
- `apply` (Boolean) When true (default), Profile.Apply runs after set so rules become live nftables. Set false to stage the profile file only.

<a id="nestedblock--adapter"></a>
### Nested Schema for `adapter`

Required:

- `name` (String) Adapter name (`global`, `eth0`, …).
- `policy` (String) Default policy for the adapter (`none`, `allow`, `drop`).

Optional:

- `rule` (Block List) Ordered firewall rules (first match wins). (see [below for nested schema](#nestedblock--adapter--rule))

<a id="nestedblock--adapter--rule"></a>
### Nested Schema for `adapter.rule`

Required:

- `policy` (String) Rule action: `allow` or `drop`.

Optional:

- `enable` (Boolean) Whether the rule is enabled.
- `log` (Boolean) Whether matching traffic is logged.
- `name` (String) Optional rule display name.
- `port_direction` (String) Port direction (`destination` / `source`).
- `port_group` (String) Port group (`custom`, built-in app lists, etc.).
- `ports` (String) Comma-separated ports or ranges when port_group is custom.
- `protocol` (String) Protocol (`tcp`, `udp`, `all`, …).
- `source_ip` (String) Source address (`all`, IP, or network).
- `source_ip_group` (String) Source type (`all`, `netmask`, `ip`, `geoip`, …).